# Contextable MCP Server Environment Variables

# ============================================
# LOCAL MODE (default)
# ============================================
# No configuration required - uses SQLite at ~/.contextable/data.db

# Optional: Override data directory
# CONTEXTABLE_DATA_DIR=~/.contextable

# Optional: Override database path
# CONTEXTABLE_DB_PATH=~/.contextable/data.db

# ============================================
# HOSTED MODE (Supabase)
# ============================================
# Required for hosted mode:
# CONTEXTABLE_API_KEY=ctx_your_api_key_here
# SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Optional: Override API URL (defaults to https://api.contextable.me)
# CONTEXTABLE_API_URL=https://api.contextable.me

# ============================================
# TRANSPORT OPTIONS
# ============================================
# HTTP/SSE transport settings (only used with --sse flag)
# CONTEXTABLE_PORT=3000
# CONTEXTABLE_HOST=127.0.0.1

# ============================================
# LOGGING
# ============================================
# Log level: debug, info, warn, error
# CONTEXTABLE_LOG_LEVEL=info
